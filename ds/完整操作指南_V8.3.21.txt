━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
V8.3.21 完整操作指南
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📅 更新时间: 2025-11-11
🔧 适用服务器: 43.100.52.142 (root用户)
📂 工作目录: /root/10-23-bot

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 目录

1. 快速命令速查
2. 修复DeepSeek配置
3. 运行回测
4. 生成历史数据
5. 检查和清理进程
6. 常见问题

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 1. 快速命令速查

### 📋 最常用命令

# 更新快速重启脚本
cd /root/10-23-bot && git pull origin main && cd ds && bash 更新快速重启脚本.sh

# 修复DeepSeek配置并重启
cd /root/10-23-bot && git pull origin main && cd ds && bash 服务器修复并重启DeepSeek.sh

# 运行回测（Qwen + DeepSeek）
bash ~/快速重启_修复版.sh backtest

# 重新生成历史数据（从指定日期）
cd /root/10-23-bot && git pull origin main && cd ds && bash 重新生成历史数据.sh 20251025

# 检查和清理多余进程
cd /root/10-23-bot && git pull origin main && cd ds && bash 检查并清理进程.sh

# 查看服务状态
bash ~/快速重启_修复版.sh status

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 2. 修复DeepSeek配置

### 问题描述
DeepSeek文件错误使用了Qwen的配置

### 已修复内容
✅ 环境文件: .env.deepseek → .env
✅ 模型名称: deepseek-chat → deepseek-reasoner
✅ 客户端: qwen_client → deepseek_client
✅ API端点: 阿里云 → DeepSeek官方
✅ 数据目录: trading_data/qwen → trading_data/deepseek
✅ 通知标签: [通义千问] → [DeepSeek]
✅ Bark组名: Qwen → DeepSeek

### 执行修复
cd /root/10-23-bot && git pull origin main && cd ds && bash 服务器修复并重启DeepSeek.sh

### 验证成功标志
✅ supervisorctl status deepseek 显示 RUNNING
✅ Bark通知显示 "[DeepSeek]启动"
✅ 数据保存到 trading_data/deepseek/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 3. 运行回测

### 方法1: 使用快速重启脚本（推荐）
bash ~/快速重启_修复版.sh backtest

### 方法2: 使用手动回测脚本
cd /root/10-23-bot/ds && bash 手动回测.sh

### 方法3: 使用一体化脚本
cd /root/10-23-bot/ds && bash 服务器回测_不停AI.sh

### 回测流程
1. 停止AI服务（方法3不停止）
2. 设置MANUAL_BACKTEST=true环境变量
3. 依次运行Qwen和DeepSeek回测
4. 显示结果总结
5. 发送邮件和Bark通知

### 回测时间
- Qwen: 5-10分钟
- DeepSeek: 5-10分钟
- 总计: 10-20分钟

### 验证回测模式
✅ 显示 "🔬 手动回测模式"
✅ Bark通知: [Qwen]🔬回测开始
✅ 不显示 "⚠️ 实盘模式"

❌ 显示 "⚠️ 实盘模式"（说明未进入回测模式）
❌ Bark通知: [DeepSeek]启动🔴

### 回测完成后
1. 检查邮箱 - 详细回测报告
2. 检查Bark - 回测完成通知
3. 查看新参数:
   cat /root/10-23-bot/ds/trading_data/qwen/config.json | grep -A 5 scalping_params
   cat /root/10-23-bot/ds/trading_data/deepseek/config.json | grep -A 5 scalping_params

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 4. 生成历史数据

### 旧命令（之前的服务器）
cd /home/admin/10-23-bot/ds
venv/bin/python export_historical_data.py 20251025

### 新命令（当前服务器）

方式1: 一键脚本（推荐）
cd /root/10-23-bot && git pull origin main && cd ds && bash 重新生成历史数据.sh 20251025

方式2: 直接Python命令
cd /root/10-23-bot/ds
python3 export_historical_data.py 20251025

### 参数说明
- 20251025: 开始日期（会自动生成到今天）
- 不提供日期: 默认生成最近14天

### 生成时间
- 每天: 10-30秒
- 14天: 3-5分钟
- 30天: 10-15分钟

### 数据位置
- 快照: trading_data/market_snapshots/
- 备份: trading_data/market_snapshots_backup_*/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 5. 检查和清理进程

### 检查进程状态
cd /root/10-23-bot && git pull origin main && cd ds && bash 检查并清理进程.sh

### 手动检查命令
# 查看Supervisor状态
supervisorctl status

# 查看所有Python进程
ps aux | grep python | grep -v grep

# 查看Qwen进程
ps aux | grep qwen_多币种智能版.py | grep -v grep

# 查看DeepSeek进程
ps aux | grep deepseek_多币种智能版.py | grep -v grep

### 正常进程配置
- Qwen AI: 1个
- DeepSeek AI: 1个
- 前端: 1个
- 总计: 3个核心进程

### 清理多余进程
# 使用脚本（推荐）
cd /root/10-23-bot/ds && bash 检查并清理进程.sh

# 手动停止进程
kill -9 <PID>

# 重启服务
supervisorctl restart qwen
supervisorctl restart deepseek

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 6. 常见问题

### Q1: 回测命令启动的是实盘而非回测？
A: 更新快速重启脚本
   cd /root/10-23-bot && git pull origin main && cd ds && bash 更新快速重启脚本.sh

### Q2: DeepSeek无法启动？
A: 检查配置是否正确
   cd /root/10-23-bot && git pull origin main && cd ds && bash 服务器修复并重启DeepSeek.sh

### Q3: 发现多个Qwen或DeepSeek进程？
A: 清理多余进程
   cd /root/10-23-bot/ds && bash 检查并清理进程.sh

### Q4: 回测卡住不动？
A: 检查进程状态
   ps aux | grep python | grep -E "qwen|deepseek" | grep -v grep
   如果CPU使用率很低且长时间无输出，可能需要重启

### Q5: 历史数据生成失败？
A: 检查：
   1. 网络连接（需要访问币安API）
   2. .env文件配置
   3. 错误日志

### Q6: 如何查看日志？
A: 
   # Qwen日志
   tail -f /var/log/supervisor/qwen-stdout.log
   
   # DeepSeek日志
   tail -f /var/log/supervisor/deepseek-stdout.log

### Q7: 如何停止所有服务？
A: bash ~/快速重启_修复版.sh stop

### Q8: 如何重启所有服务？
A: bash ~/快速重启_修复版.sh all

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 完整工作流程

### 日常维护流程
1. 拉取最新代码
   cd /root/10-23-bot && git pull origin main

2. 检查服务状态
   bash ~/快速重启_修复版.sh status

3. 如有问题，检查进程
   cd ds && bash 检查并清理进程.sh

### 回测流程
1. 确保历史数据完整
   cd /root/10-23-bot/ds && bash 重新生成历史数据.sh

2. 运行回测
   bash ~/快速重启_修复版.sh backtest

3. 等待10-20分钟

4. 检查邮件和Bark通知

5. 验证新参数
   cat /root/10-23-bot/ds/trading_data/qwen/config.json | grep -A 5 scalping_params

### 问题排查流程
1. 查看服务状态
   supervisorctl status

2. 查看进程
   ps aux | grep python | grep -E "qwen|deepseek" | grep -v grep

3. 查看日志
   tail -50 /var/log/supervisor/qwen-stdout.log
   tail -50 /var/log/supervisor/deepseek-stdout.log

4. 如有错误，重启服务
   supervisorctl restart qwen deepseek

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 重要提醒

⚠️  回测时AI服务会继续运行（使用"服务器回测_不停AI.sh"）
⚠️  回测需要10-20分钟，请耐心等待
⚠️  回测完成后检查邮件和Bark通知
⚠️  定期检查和清理多余进程
⚠️  每次更新后先git pull再执行脚本

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 相关文档

- DeepSeek配置对照表: V8.3.21_DeepSeek配置对照表.md
- DeepSeek修复报告: V8.3.21_DeepSeek紧急修复报告.md
- 回测优化说明: V8.3.21_回测优化方案.md
- 最终版本说明: V8.3.21_最终版本说明.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

更新日期: 2025-11-11
版本: V8.3.21
维护: AI Assistant (Claude Sonnet 4.5)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

