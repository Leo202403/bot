━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
V8.3.21 DeepSeek最终修复（已验证）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  问题：DeepSeek配置有2处关键错误

✅ 已修正（对照GitHub原始配置）：
  1. 环境文件：.env.deepseek ❌ → .env ✅
  2. 模型名称：deepseek-chat ❌ → deepseek-reasoner ✅

✅ 验证通过的配置：
  • 环境文件: .env
  • 环境变量: DEEPSEEK_API_KEY
  • 客户端: deepseek_client
  • API端点: https://api.deepseek.com
  • 模型名称: deepseek-reasoner (15处)
  • 数据目录: trading_data/deepseek
  • Bark组名: DeepSeek
  • 通知标签: [DeepSeek]
  • Qwen残留: 0处

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 在服务器上执行（一键命令）：

cd /root/10-23-bot && git pull origin main && cd ds && bash 服务器修复并重启DeepSeek.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 验证成功的标志：

1. supervisorctl status deepseek 显示 RUNNING
2. 无 "找不到 .env.deepseek 文件" 错误
3. 无 "QWEN_API_KEY 环境变量未设置" 错误
4. Bark通知显示 "[DeepSeek]启动"
5. 数据保存到 trading_data/deepseek/ 目录
6. 日志显示使用 deepseek-reasoner 模型

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 DeepSeek vs Qwen 配置差异：

┌─────────────┬──────────────────────────┬─────────────────────┐
│   配置项    │          Qwen            │      DeepSeek       │
├─────────────┼──────────────────────────┼─────────────────────┤
│ 环境文件    │ .env.qwen                │ .env                │
│ 环境变量    │ QWEN_API_KEY             │ DEEPSEEK_API_KEY    │
│ API端点     │ 阿里云                    │ DeepSeek官方        │
│ 模型名称    │ qwen3-max                │ deepseek-reasoner   │
│ 数据目录    │ trading_data/qwen        │ trading_data/deepseek│
│ Bark组名    │ Qwen                     │ DeepSeek            │
│ 通知标签    │ [通义千问]                │ [DeepSeek]          │
└─────────────┴──────────────────────────┴─────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️  预计时间：2-3分钟（不含回测）

📝 关键注意：
  • DeepSeek使用通用的 .env 文件（不是 .env.deepseek）
  • DeepSeek使用 deepseek-reasoner 模型（不是 deepseek-chat）
  • deepseek-reasoner 是推理模型，有 <think> 标签

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

