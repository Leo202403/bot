═══════════════════════════════════════════════════════════════════
立即部署 - V8.2.6.8 完整修复（consensus最小值2）
═══════════════════════════════════════════════════════════════════

【关键发现】
❌ V8.2.6.4的修复不完整！
   只修改了"默认范围"，但AI每次运行会保存"历史范围"
   下次运行时读取历史范围，如果包含1，就继续使用1！

【V8.2.6.8修复】
✅ 在2处强制consensus最小值为2：
   1. 保存历史范围时（第5795行）
   2. 读取历史范围时（第5918行）

═══════════════════════════════════════════════════════════════════
部署步骤（服务器）
═══════════════════════════════════════════════════════════════════

# 1. 上传修复后的文件
scp deepseek_多币种智能版.py qwen_多币种智能版.py admin@服务器IP:/home/admin/10-23-bot/ds/

# 2. SSH到服务器
ssh admin@服务器IP

# 3. 删除旧配置（重要！清除包含consensus=1的历史范围）
cd ~/10-23-bot/ds/trading_data
rm -f deepseek/learning_config.json
rm -f qwen/learning_config.json

# 4. 重启AI
cd ~/10-23-bot/ds
ps aux | grep '多币种智能版.py' | grep -v grep | awk '{print $2}' | xargs -r kill -9
nohup venv/bin/python deepseek_多币种智能版.py > logs/deepseek_trading.log 2>&1 &
nohup venv/bin/python qwen_多币种智能版.py > logs/qwen_trading.log 2>&1 &

# 5. 验证修复（5分钟后）
tail -100 logs/deepseek_trading.log | grep "consensus_range"
# 应该看到：consensus_range: [2, X]（不应该是[1, X]）

═══════════════════════════════════════════════════════════════════
修复说明
═══════════════════════════════════════════════════════════════════

【问题根源】
AI每次回测后会保存"最优采样范围"：
```python
# 旧代码（V8.2.6.4）
'consensus_range': [min(consensus_values), max(consensus_values)]
                    ↑ 如果包含1，就会保存[1, 3]
```

下次运行时，读取历史范围：
```python
# 旧代码（V8.2.6.4）
cons_range = historical_sampling_range['consensus_range']
             ↑ 直接使用[1, 3]，没有强制修正
```

结果：即使默认范围都设为≥2，AI仍会通过"历史范围"使用1！

【V8.2.6.8修复】
保存时强制最小值为2：
```python
# 新代码（V8.2.6.8）- 第5795行
'consensus_range': [max(2, min(consensus_values)), max(consensus_values)]
                    ↑ 即使包含1，也会保存[2, X]
```

读取时强制最小值为2：
```python
# 新代码（V8.2.6.8）- 第5918行
cons_range = historical_sampling_range['consensus_range']
cons_range = [max(2, cons_range[0]), max(2, cons_range[1])]
             ↑ 即使历史是[1, 3]，也会修正为[2, 3]
```

═══════════════════════════════════════════════════════════════════
预期效果（部署后）
═══════════════════════════════════════════════════════════════════

指标                    当前          V8.2.6.8
──────────────────────────────────────────────
min_indicator_consensus  1         ✅ 2（最小）
超短线捕获率            96-99%    ✅ 75-85%
波段捕获率              99%       ✅ 35-45%

═══════════════════════════════════════════════════════════════════
验证清单（24小时后）
═══════════════════════════════════════════════════════════════════

✅ 查看learning_config.json：
   cd ~/10-23-bot/ds/trading_data/deepseek
   cat learning_config.json | grep -A 5 "consensus_range"
   # 应该显示 [2, X]，不是 [1, X]

✅ 查看明天的邮件报告：
   - min_indicator_consensus应该 ≥ 2
   - 超短线捕获率应该 75-85%（不是96%）
   - 波段捕获率应该 35-45%（不是99%）

✅ 查看优化日志：
   tail -200 logs/deepseek_trading.log | grep "✓ min_indicator_consensus"
   # 应该显示：2 → 2或3（不应该有 → 1）

═══════════════════════════════════════════════════════════════════
常见问题
═══════════════════════════════════════════════════════════════════

Q: 为什么要删除learning_config.json？
A: 因为旧的配置文件包含consensus_range: [1, 3]
   如果不删除，AI会读取并使用这个范围
   虽然新代码会修正它，但删除更彻底

Q: 捕获率从96%降到75%，不是变差了吗？
A: 不是！96%是因为consensus=1太宽松，抓了太多低质量机会
   75-85%是合理范围，质量更高，实际利润更好

Q: 波段为什么只显示1条？
A: 这是另一个问题（V8.2.6.7的机会分类逻辑）
   需要单独验证，但现在先修复consensus问题

═══════════════════════════════════════════════════════════════════

版本：V8.2.6.8
日期：2025-11-07
优先级：🔴 紧急（立即部署）

