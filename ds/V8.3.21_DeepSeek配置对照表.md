# V8.3.21 DeepSeek vs Qwen 配置对照表

## 📊 完整配置对照

| 配置项 | Qwen | DeepSeek | 状态 |
|--------|------|----------|------|
| **环境文件** | `.env.qwen` | `.env` | ✅ |
| **环境变量名** | `QWEN_API_KEY` | `DEEPSEEK_API_KEY` | ✅ |
| **客户端对象** | `qwen_client` | `deepseek_client` | ✅ |
| **API端点** | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `https://api.deepseek.com` | ✅ |
| **模型名称** | `qwen3-max` | `deepseek-reasoner` | ✅ |
| **数据目录** | `trading_data/qwen` | `trading_data/deepseek` | ✅ |
| **Bark组名** | `Qwen` | `DeepSeek` | ✅ |
| **通知标签** | `[通义千问]` | `[DeepSeek]` | ✅ |
| **邮件model_name默认值** | `Qwen` | `DeepSeek` | ✅ |
| **MODEL_NAME环境变量默认** | `qwen` | `deepseek` | ✅ |

---

## 🔧 关键修复说明

### 1. 环境文件路径
```python
# Qwen
_env_file = Path(__file__).parent / '.env.qwen'

# DeepSeek
_env_file = Path(__file__).parent / '.env'  # ✅ 不是 .env.deepseek
```

**原因**：DeepSeek使用通用的`.env`文件，而Qwen使用专用的`.env.qwen`文件。

---

### 2. 模型名称
```python
# Qwen
model="qwen3-max"

# DeepSeek
model="deepseek-reasoner"  # ✅ 不是 deepseek-chat
```

**原因**：
- `deepseek-reasoner`：推理模型，带有`<think></think>`标签，更适合复杂决策
- `deepseek-chat`：对话模型，没有推理过程

---

### 3. API端点
```python
# Qwen
base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"

# DeepSeek
base_url="https://api.deepseek.com"
```

**原因**：Qwen使用阿里云API，DeepSeek使用官方API。

---

### 4. 注释和文档字符串
```python
# Qwen
"""支持Qwen模型的推理标签"""

# DeepSeek
"""支持DeepSeek Reasoner的推理标签"""  # ✅ 强调Reasoner
```

---

## ✅ 验证清单

### 必须检查的项目
- [x] 环境文件：`.env` (不是 `.env.deepseek`)
- [x] 环境变量：`DEEPSEEK_API_KEY`
- [x] 客户端名称：`deepseek_client`
- [x] API端点：`https://api.deepseek.com`
- [x] 模型名称：`deepseek-reasoner` (共15处)
- [x] 数据目录：`trading_data/deepseek`
- [x] Bark组名：`DeepSeek`
- [x] 通知标签：`[DeepSeek]`
- [x] 无Qwen残留引用

---

## 🎯 两次修复总结

### 第一次修复（错误）
```python
# ❌ 错误配置
_env_file = Path(__file__).parent / '.env.deepseek'  # 错误：应该是 .env
model="deepseek-chat"  # 错误：应该是 deepseek-reasoner
```

**问题**：错误地参考了Qwen的文件命名模式（`.env.qwen`），以为DeepSeek应该用`.env.deepseek`。

### 第二次修复（正确）
```python
# ✅ 正确配置
_env_file = Path(__file__).parent / '.env'  # DeepSeek使用通用.env
model="deepseek-reasoner"  # DeepSeek使用Reasoner推理模型
```

**验证方法**：对照GitHub历史版本（commit 7436f69）确认配置正确性。

---

## 📋 服务器部署命令

### 一键修复和重启
```bash
cd /root/10-23-bot && git pull origin main && cd ds && bash 服务器修复并重启DeepSeek.sh
```

### 验证启动成功
```bash
# 1. 检查服务状态
supervisorctl status deepseek

# 2. 检查最新日志
tail -30 /var/log/supervisor/deepseek-stdout.log

# 3. 验证Bark通知
# 手机应收到 "[DeepSeek]启动" 通知（不是"[通义千问]"）

# 4. 验证数据目录
ls -la /root/10-23-bot/ds/trading_data/deepseek/
```

---

## 🔍 Git提交历史

```bash
# 第1次提交：清理Qwen引用（但有2处错误）
commit 249f84d
V8.3.21: 修复DeepSeek文件中错误的Qwen引用
- 79处修改，但.env路径和模型名称不正确

# 第2次提交：修正关键配置
commit b169af1
V8.3.21: 修正DeepSeek关键配置（.env路径和deepseek-reasoner模型）
- 修正.env文件路径
- 修正模型名称为deepseek-reasoner
```

---

## 💡 经验教训

### 1. 不要想当然地推断
- ❌ 错误思维：Qwen用`.env.qwen`，所以DeepSeek应该用`.env.deepseek`
- ✅ 正确做法：查看GitHub历史版本，确认原始配置

### 2. 参考官方文档
- `deepseek-reasoner`：推理模型，适合复杂决策
- `deepseek-chat`：对话模型，适合简单对话

### 3. 完整的验证流程
1. 环境文件路径
2. 环境变量名称
3. API端点
4. 模型名称
5. 数据目录
6. 通知配置
7. 注释和文档

---

## ✅ 最终状态

| 项目 | 状态 |
|------|------|
| 环境文件路径 | ✅ `.env` |
| 模型名称 | ✅ `deepseek-reasoner` (15处) |
| API端点 | ✅ `https://api.deepseek.com` |
| 数据目录 | ✅ `trading_data/deepseek` |
| Bark组名 | ✅ `DeepSeek` |
| 通知标签 | ✅ `[DeepSeek]` |
| Qwen残留 | ✅ 0处 |
| **总体状态** | **✅ 100%正确** |

---

**修复完成时间**：2025-11-11 19:15  
**验证方式**：对照GitHub历史 + 完整配置检查  
**下一步**：服务器部署和回测验证

