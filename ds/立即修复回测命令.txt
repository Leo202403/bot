━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
V8.3.21 立即修复回测命令
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  问题：bash ~/快速重启_修复版.sh backtest 启动的是实盘而非回测

✅ 原因：脚本缺少 MANUAL_BACKTEST=true 环境变量

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 在服务器上执行（一键修复）：

cd /root/10-23-bot && git pull origin main && cd ds && bash 更新快速重启脚本.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 修复内容：

1. 添加环境变量设置
   export MANUAL_BACKTEST=true

2. 正确触发回测模式
   - Qwen回测模式
   - DeepSeek回测模式

3. 回测完成后显示总结
   - 两个模型的退出状态
   - 成功/失败统计

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 修复后的使用方法：

# 运行回测（Qwen + DeepSeek）
bash ~/快速重启_修复版.sh backtest

# 重启Qwen
bash ~/快速重启_修复版.sh qwen

# 重启DeepSeek
bash ~/快速重启_修复版.sh deepseek

# 查看状态
bash ~/快速重启_修复版.sh status

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 验证回测模式正确启动的标志：

✅ 正确（回测模式）：
  • 显示 "🔬 手动回测模式 - 立即触发参数优化"
  • Bark通知：[Qwen]🔬回测开始 或 [DeepSeek]🔬回测开始
  • 不显示 "⚠️ 实盘模式，请谨慎！"

❌ 错误（实盘模式）：
  • 显示 "⚠️ 实盘模式，请谨慎！"
  • Bark通知：[Qwen]启动🔴 或 [DeepSeek]启动🔴
  • 不显示回测相关信息

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️  预计时间：

• 更新脚本：30秒
• 运行回测：5-10分钟（每个模型）
• 总计：10-20分钟

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 回测完成后：

1. 检查邮箱
   - 详细回测报告
   - 参数对比
   - AI建议

2. 检查手机Bark通知
   - 回测完成通知
   - 新参数应用通知

3. 查看新参数
   cat /root/10-23-bot/ds/trading_data/qwen/config.json | grep -A 5 scalping_params
   cat /root/10-23-bot/ds/trading_data/deepseek/config.json | grep -A 5 scalping_params

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

