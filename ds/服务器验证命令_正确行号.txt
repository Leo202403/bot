═══════════════════════════════════════════════════════════
V8.3.4 服务器验证（正确行号）
═══════════════════════════════════════════════════════════

在服务器执行：

# 1. ✅ 验证V8.3.4函数存在
grep -n "def run_separated_optimization_with_opportunities" deepseek_多币种智能版.py

【预期】3097:def run_separated_optimization_with_opportunities(opportunity_analysis, config):


# 2. ✅ 验证V8.3.0调用位置（正确位置：8639行，不是8503行！）
sed -n '8637,8650p' deepseek_多币种智能版.py

【预期】
        # ========== 🚀 V8.3.0: Separated Strategy Optimization ==========
        print("\n【V8.3.0: Separated Strategy Optimization】")
        if opportunity_analysis and opportunity_analysis.get('all_opportunities'):
            try:
                # Run separated optimization using objective opportunities
                run_separated_optimization_with_opportunities(opportunity_analysis, config)


# 3. ✅ 验证波段显示缩进（正确位置：7775行，不是7640行！）
sed -n '7772,7780p' deepseek_多币种智能版.py

【预期】
                            elif old_can_entry and not new_can_entry:
                                analysis = '⚪ 新参数略严格（质量更优）'
                                row_bg = 'background: #f5f5f5;'
                            else:
                                miss_reason = opp.get('miss_reason', '信号质量不足')
                                analysis = miss_reason if miss_reason else '信号质量不足'
                                row_bg = 'background: #ffebee;'
                            
                            opportunity_stats_html += f'''


# 4. ✅ 验证行数
wc -l *多币种智能版.py

【预期】
  17903 deepseek_多币种智能版.py
  17902 qwen_多币种智能版.py


# 5. ✅ 验证consensus修复
grep "min C=2, NEVER" deepseek_多币种智能版.py

【预期】Strategy: Adjust R:R±0.1-0.3, ATR±0.05-0.15, C±1 (min C=2, NEVER use C=1)


═══════════════════════════════════════════════════════════
总结：你的服务器代码应该是正确的V8.3.4！
═══════════════════════════════════════════════════════════

1. ✅ 行数正确（17903和17902）
2. ✅ V8.3.4标记存在（3099行和3111行）
3. ✅ consensus修复存在

之前验证脚本的行号写错了，导致误判。
现在可以继续：

# 6. 重启AI
cd ~/10-23-bot/ds
ps aux | grep '多币种智能版.py' | grep -v grep | awk '{print $2}' | xargs -r kill -9
nohup venv/bin/python deepseek_多币种智能版.py > logs/deepseek_trading.log 2>&1 &
nohup venv/bin/python qwen_多币种智能版.py > logs/qwen_trading.log 2>&1 &

# 7. 手动回测验证
bash 快速重启_修复版.sh backtest
