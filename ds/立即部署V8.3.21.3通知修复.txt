# V8.3.21.3 通知修复 - 服务器部署指南

## 快速部署（复制执行）

```bash
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# V8.3.21.3 通知修复 - 一键部署
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1️⃣ 拉取最新代码
cd ~/10-23-bot && git pull origin main

# 2️⃣ 语法验证
cd ds
python3 -m py_compile deepseek_多币种智能版.py
python3 -m py_compile qwen_多币种智能版.py

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ 语法验证通过"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# 3️⃣ 重启AI服务
supervisorctl restart qwen deepseek

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔄 AI服务已重启"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# 4️⃣ 验证服务状态
supervisorctl status | grep -E "(qwen|deepseek)"

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📊 服务状态检查"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# 5️⃣ 查看启动日志（最后20行）
echo ""
echo "📝 Qwen 启动日志（最后20行）:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
tail -20 /root/10-23-bot/ds/trading_data/qwen/error.log

echo ""
echo "📝 DeepSeek 启动日志（最后20行）:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
tail -20 /root/10-23-bot/ds/trading_data/deepseek/error.log

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ V8.3.21.3部署完成"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "🔍 下一步验证:"
echo "   1. 手动回测: bash ~/快速重启_修复版.sh backtest"
echo "   2. 等待自动回测: 每日08:05（北京时间）"
echo "   3. 检查Bark通知是否显示'V8.3.21优化'"
echo "   4. 检查邮件是否有绿色边框的V8.3.21板块"
echo ""
```

---

## 详细步骤说明

### 步骤1: 拉取最新代码
```bash
cd ~/10-23-bot && git pull origin main
```

**预期输出**:
```
remote: Enumerating objects: X, done.
...
From https://github.com/Leo202403/bot
   48bfc1a..3459f90  main       -> origin/main
Updating 48bfc1a..3459f90
Fast-forward
 ds/deepseek_多币种智能版.py | 78 +++++++++++++++---
 ds/qwen_多币种智能版.py     | 78 +++++++++++++++---
 ...
```

---

### 步骤2: 语法验证
```bash
cd ds
python3 -m py_compile deepseek_多币种智能版.py
python3 -m py_compile qwen_多币种智能版.py
```

**预期输出**: 无输出（表示语法正确）

**如果有错误**:
```bash
# 检查具体错误信息
python3 deepseek_多币种智能版.py 2>&1 | head -20
```

---

### 步骤3: 重启AI服务
```bash
supervisorctl restart qwen deepseek
```

**预期输出**:
```
qwen: stopped
deepseek: stopped
qwen: started
deepseek: started
```

---

### 步骤4: 验证服务状态
```bash
supervisorctl status | grep -E "(qwen|deepseek)"
```

**预期输出**:
```
qwen                             RUNNING   pid 12345, uptime 0:00:05
deepseek                         RUNNING   pid 12346, uptime 0:00:05
```

**如果状态为FATAL或BACKOFF**:
```bash
# 查看错误日志
tail -50 /root/10-23-bot/ds/trading_data/qwen/error.log
tail -50 /root/10-23-bot/ds/trading_data/deepseek/error.log
```

---

### 步骤5: 查看启动日志
```bash
# Qwen
tail -20 /root/10-23-bot/ds/trading_data/qwen/error.log

# DeepSeek
tail -20 /root/10-23-bot/ds/trading_data/deepseek/error.log
```

**预期日志**:
```
🔧 币安交易所初始化: 统一账户模式 (papi)
✓ 时间同步正常 (差异XXms)
======================================================================
多币种AI智能交易系统启动
======================================================================
监控币种: BTC, ETH, SOL, BNB, XRP, DOGE, LTC
...
```

---

## 手动回测验证

### 触发回测
```bash
cd ~/10-23-bot && bash ~/快速重启_修复版.sh backtest
```

### 验证V8.3.21数据已保存
```bash
# 检查Qwen的V8.3.21洞察
cat /root/10-23-bot/ds/trading_data/qwen/learning_config.json | jq '.compressed_insights.v8321_insights'

# 检查DeepSeek的V8.3.21洞察
cat /root/10-23-bot/ds/trading_data/deepseek/learning_config.json | jq '.compressed_insights.v8321_insights'
```

**预期输出**:
```json
{
  "scalping": {
    "performance": {
      "score": 0.848,
      "capture_rate": 0.74,
      "avg_profit": 12.0
    },
    "best_contexts": [
      "阳线比例0.7-0.8时效果最好（平均利润12.6%）",
      "LL-LH结构效果最好（平均利润12.6%）"
    ],
    "updated_at": "2025-11-11 XX:XX:XX"
  },
  "swing": {
    "performance": {
      "score": 0.843,
      "capture_rate": 0.71,
      "avg_profit": 20.6
    },
    "best_contexts": [
      "阳线比例0.7-0.8时效果最好（平均利润19.8%）",
      "LL-LH结构效果最好（平均利润21.9%）"
    ],
    "updated_at": "2025-11-11 XX:XX:XX"
  }
}
```

---

## 验证Bark通知

### 预期格式
```
[通义千问]🤖AI参数优化V8.3.21
胜率55% 盈亏比0.9
多轮迭代1轮
📊V8.3.21优化:⚡0.85分 74%捕获 🌊0.84分 71%捕获
```

或

```
[DeepSeek]🤖AI参数优化V8.3.21
胜率55% 盈亏比0.8
多轮迭代1轮
📊V8.3.21优化:⚡0.85分 74%捕获 🌊0.84分 71%捕获
```

### 检查点
- ✅ 标题包含"V8.3.21"（不再是"V7.7.0"）
- ✅ 内容包含"📊V8.3.21优化:"
- ✅ 显示"⚡X.XX分 XX%捕获"（超短线）
- ✅ 显示"🌊X.XX分 XX%捕获"（波段）

---

## 验证邮件报告

### 预期内容（邮箱查看）

#### 1️⃣ 顶部有绿色边框板块
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 V8.3.21 回测优化结果（实际运行参数）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 以下数据来自V8.3.21增强优化器的真实回测结果，
   已应用于实时交易决策
```

#### 2️⃣ 超短线策略数据
```
⚡ 超短线策略

优化得分: 0.848
捕获率: 74% | 平均利润: 12.0%

🔑 最优市场上下文（Top 2）:
• 阳线比例0.7-0.8时效果最好（平均利润12.6%）
• LL-LH结构效果最好（平均利润12.6%）
```

#### 3️⃣ 波段策略数据
```
🌊 波段策略

优化得分: 0.843
捕获率: 71% | 平均利润: 20.6%

🔑 最优市场上下文（Top 2）:
• 阳线比例0.7-0.8时效果最好（平均利润19.8%）
• LL-LH结构效果最好（平均利润21.9%）
```

---

## 问题排查

### 问题1: Bark通知仍显示旧格式
**症状**: 通知标题仍是"V7.7.0"，内容无"📊V8.3.21优化:"

**原因**: `v8321_insights`未生成

**排查**:
```bash
# 检查v8321_insights是否存在
cat /root/10-23-bot/ds/trading_data/qwen/learning_config.json | jq '.compressed_insights.v8321_insights'

# 如果为null，手动触发回测
cd ~/10-23-bot && bash ~/快速重启_修复版.sh backtest
```

---

### 问题2: 邮件无绿色板块
**症状**: 邮件中没有"🎯 V8.3.21 回测优化结果"板块

**原因**: 邮件HTML渲染问题或数据未读取

**排查**:
```bash
# 查看回测日志中的邮件调试信息
tail -100 /root/10-23-bot/ds/trading_data/qwen/output.log | grep "邮件调试"

# 预期输出
[邮件调试] compressed_insights 存在: True
[邮件调试] v8321_insights 存在: True
[邮件调试] v8321_insights 内容: {'scalping': {...}, 'swing': {...}}
```

---

### 问题3: 数据不一致
**症状**: Bark显示74%捕获，但邮件显示30%

**原因**: 邮件读取了缓存或旧版数据

**排查**:
```bash
# 对比learning_config.json中的数据
cat /root/10-23-bot/ds/trading_data/qwen/learning_config.json | jq '{
  v8321_scalp_capture: .compressed_insights.v8321_insights.scalping.performance.capture_rate,
  v8321_swing_capture: .compressed_insights.v8321_insights.swing.performance.capture_rate
}'

# 预期输出
{
  "v8321_scalp_capture": 0.74,
  "v8321_swing_capture": 0.71
}
```

---

## 监控建议

### 每日回测后检查
1. **Bark通知**: 截图保存，确认V8.3.21标识
2. **邮件报告**: 查看V8.3.21板块，记录得分变化
3. **日志对比**: `grep "V8.3.21优化完成" output.log`

### 数据趋势记录
```bash
# 创建简单的监控脚本
cat > ~/check_v8321_trend.sh << 'EOF'
#!/bin/bash
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "V8.3.21 优化趋势监控"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "📊 Qwen:"
cat /root/10-23-bot/ds/trading_data/qwen/learning_config.json | jq '{
  scalping: .compressed_insights.v8321_insights.scalping.performance,
  swing: .compressed_insights.v8321_insights.swing.performance
}'
echo ""
echo "📊 DeepSeek:"
cat /root/10-23-bot/ds/trading_data/deepseek/learning_config.json | jq '{
  scalping: .compressed_insights.v8321_insights.scalping.performance,
  swing: .compressed_insights.v8321_insights.swing.performance
}'
EOF

chmod +x ~/check_v8321_trend.sh
```

**使用**:
```bash
~/check_v8321_trend.sh
```

---

## 成功标志

- [x] 代码拉取成功
- [x] 语法验证通过
- [x] AI服务重启成功
- [ ] Bark通知显示"V8.3.21"
- [ ] Bark通知显示"📊V8.3.21优化:⚡XX分 XX%捕获 🌊XX分 XX%捕获"
- [ ] 邮件报告有绿色边框的V8.3.21板块
- [ ] 邮件报告显示优化得分、捕获率、平均利润
- [ ] 邮件报告显示最优市场上下文（Top 2）
- [ ] Bark/邮件/日志数据一致

---

## 联系支持

如遇到问题，请提供:
1. 服务器IP和用户名
2. 错误日志（最后50行）
3. `learning_config.json`内容（`v8321_insights`部分）
4. Bark通知截图（如有）
5. 邮件报告截图（如有）

**版本**: V8.3.21.3  
**日期**: 2025-11-11  
**状态**: 待部署验证

