━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
检查 Qwen AI 决策文件是否损坏
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 问题分析：
   DeepSeek 错误使用 Qwen 配置期间，可能把 Qwen 的 ai_decisions.json 
   文件格式搞乱了，导致：
   - 综合页面只显示 DeepSeek 决策
   - Qwen 决策不在综合页面显示
   - Qwen 单独页面也不显示决策

📋 在服务器上执行以下一键命令：

cd /root/10-23-bot && git pull origin main && bash ds/检查Qwen决策文件.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 检查内容：
   1. Qwen 和 DeepSeek 的决策文件是否存在
   2. 文件大小、行数对比
   3. JSON 格式是否有效
   4. 数据类型（应该是 list，不应该是 dict）
   5. 决策条数和最新决策的字段
   6. 与 DeepSeek 文件格式对比

⚠️  如果发现问题：
   - JSON 格式错误 → 需要修复或备份后重建
   - 数据类型是 dict 而不是 list → 需要重建
   - 决策字段缺失或异常 → 需要修复

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 执行后把完整输出发给我，我会根据结果提供修复方案！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

