━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
V8.3.21 前端修复说明 - 综合页面显示Qwen决策
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  问题现象：
  • 综合页面的AI决策不显示Qwen的实时决策
  • 切换到Qwen单独页面可以正常显示
  • DeepSeek决策在综合页面正常显示

✅ 问题根源：
  • trading-combined API调用的get_model_summary函数
  • 该函数返回的数据中缺少ai_decisions字段
  • 导致综合页面无法显示AI决策历史

🔧 修复内容：
  • 在get_model_summary函数中添加ai_decisions读取
  • 每个模型返回最后10条AI决策
  • trading-combined API现在包含两个模型的决策历史

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 在服务器上执行（一键修复）：

cd /root/10-23-bot && git pull origin main && cd ds && bash 更新前端修复.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 脚本会自动：

1. 拉取最新代码（包含修复）
2. 备份旧的前端文件
3. 复制修复后的文件到正确位置
4. 重启web服务
5. 验证服务启动成功
6. 显示最新日志

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️  预计时间：1-2分钟

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 修复后测试：

1. 打开浏览器，访问综合页面

2. 强制刷新页面
   Windows/Linux: Ctrl + F5
   Mac: Cmd + Shift + R

3. 检查AI决策部分
   应该能看到：
   • Qwen的最新决策
   • DeepSeek的最新决策
   • 每个决策都标记了来源模型

4. 如果还是不显示
   - 打开F12开发者工具
   - 查看Console标签的错误信息
   - 查看Network标签的API请求

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 API数据结构变化：

修复前（trading-combined返回）：
{
  "models": {
    "qwen": {
      "status": {...},
      "positions": [...],
      "recent_trades": [...]
      // ❌ 缺少 ai_decisions
    },
    "deepseek": {
      "status": {...},
      "positions": [...],
      "recent_trades": [...]
      // ❌ 缺少 ai_decisions
    }
  }
}

修复后（trading-combined返回）：
{
  "models": {
    "qwen": {
      "status": {...},
      "positions": [...],
      "recent_trades": [...],
      "ai_decisions": [...]  // ✅ 新增
    },
    "deepseek": {
      "status": {...},
      "positions": [...],
      "recent_trades": [...],
      "ai_decisions": [...]  // ✅ 新增
    }
  }
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 修复位置：

文件：每日壁纸更换.py
函数：get_model_summary (第1911行起)
修改：添加ai_decisions读取逻辑 (第2253-2268行)

代码：
```python
# 🆕 【V8.3.21修复】读取AI决策历史（用于综合页面显示）
try:
    decisions_file = os.path.join(data_dir, 'ai_decisions.json')
    if os.path.exists(decisions_file):
        with open(decisions_file, 'r', encoding='utf-8') as f:
            decisions = json.load(f)
            # 只返回最后10条决策，减少数据传输量
            if isinstance(decisions, list):
                summary['ai_decisions'] = decisions[-10:]
            else:
                summary['ai_decisions'] = []
    else:
        summary['ai_decisions'] = []
except Exception as e:
    logging.error(f"读取{model}AI决策历史失败: {e}")
    summary['ai_decisions'] = []
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 验证成功的标志：

1. ✅ web服务状态为RUNNING
2. ✅ 端口监听正常（gunicorn进程）
3. ✅ 日志中无错误
4. ✅ 浏览器综合页面显示两个模型的决策
5. ✅ 决策数据实时更新

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

