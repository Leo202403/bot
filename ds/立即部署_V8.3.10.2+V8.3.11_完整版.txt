======================================================================
🚀 立即部署 V8.3.10.2 + V8.3.11 - 完整修复版
======================================================================
⏰ 修复时间: 2025-11-07 18:15
📊 修复内容: Series歧义错误 + Separated Params读取

======================================================================
【修复清单】
======================================================================

✅ V8.3.10.2 (共5处) - Series布尔歧义错误
  1️⃣ analyze_opportunities_with_new_params: max_price_24h = float(...)
  2️⃣ analyze_opportunities_with_new_params: min_price_24h = float(...)
  3️⃣ _simulate_trade_with_params: close_price = float(...)
  4️⃣ _simulate_trade_with_params: high/low = float(...)
  5️⃣ _simulate_trade_with_params: last_close = float(...)

✅ V8.3.11 (共2处) - Separated Params正确读取
  1️⃣ get_learning_config_for_symbol: 添加signal_type参数，优先读取separated params
  2️⃣ calculate_unified_risk_reward_v2: 根据signal_type读取对应策略参数

======================================================================
【立即部署命令】
======================================================================

# 1. 从本地上传最新文件
cd /Users/mac-bauyu/Downloads/10-23-bot/ds
scp qwen_多币种智能版.py deepseek_多币种智能版.py admin@你的服务器IP:~/10-23-bot/ds/

# 2. SSH到服务器
ssh admin@你的服务器IP

# 3. 验证文件更新时间（应该是最新的）
cd ~/10-23-bot/ds
ls -lh *多币种智能版.py | tail -2

# 4. 验证语法
python3 -m py_compile qwen_多币种智能版.py deepseek_多币种智能版.py
echo "✅ 语法验证完成"

# 5. 重新运行回测（推荐先测DeepSeek）
cd ~
bash 快速重启_修复版.sh backtest-deepseek

======================================================================
【验证修复效果】
======================================================================

✅ V8.3.10.2修复成功标志:
  - 不再看到: "⚠️ The truth value of a Series is ambiguous"
  - 看到: "✅ V8.3.4 separated optimization completed"
  - 看到机会捕获统计正常显示

✅ V8.3.11修复成功标志:
  - learning_config.json包含separated params:
    ```json
    {
      "scalping_params": {"min_risk_reward": X, ...},
      "swing_params": {"min_risk_reward": Y, ...}
    }
    ```
  
  - Bark通知显示的要求更合理:
    ```
    Before: 要求2.6 (= 2.0×1.3，从global读取)
    After:  要求2.34 (= 1.8×1.3，从swing_params读取)
    ```

  - 实际开仓率可能提升（因为要求更合理）

======================================================================
【预期改进】
======================================================================

1. 📊 回测更准确
   - V8.3.4机会分析不再报错
   - 捕获率统计正确显示

2. 🎯 实际交易更合理
   - 使用V8.3.4优化后的separated params
   - 超短线和波段使用各自的参数

3. 📈 开仓机会可能增加
   - 如果V8.3.4优化后的参数更宽松
   - 实际要求与回测结果一致

4. 📱 Bark通知更准确
   - 显示的要求基准来自正确的策略参数
   - 更容易理解为什么被拒绝

======================================================================
【文件变更总结】
======================================================================

📁 qwen_多币种智能版.py:
  - 第3576行: get_learning_config_for_symbol 函数签名+参数读取逻辑
  - 第10537行: calculate_unified_risk_reward_v2 参数读取逻辑
  - 第17350行: max_price_24h float转换
  - 第17353行: min_price_24h float转换
  - 第17723行: close_price float转换
  - 第17730-17731行: high/low float转换
  - 第17756行: last_close float转换

📁 deepseek_多币种智能版.py:
  - (相同的7处修改)

======================================================================
✅ 本地验证: 通过 | 随时可以部署！
======================================================================

