━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 快速命令参考卡 - V8.3.25.8 Final
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 所有命令都已验证可用！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【1. 重启服务】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 重启所有服务（AI + Web + 前端）⭐ 推荐
bash ~/快速重启_修复版.sh all

# 只重启前端
bash ~/快速重启_修复版.sh frontend

# 只重启AI机器人（DeepSeek + Qwen）
bash ~/快速重启_修复版.sh bots

# 只重启DeepSeek
bash ~/快速重启_修复版.sh deepseek

# 只重启Qwen
bash ~/快速重启_修复版.sh qwen

# 只重启Web面板（等同于frontend）
bash ~/快速重启_修复版.sh web

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【2. 手动回测】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 回测所有模型（DeepSeek + Qwen）
bash ~/快速重启_修复版.sh backtest

# 只回测DeepSeek
bash ~/快速重启_修复版.sh backtest-deepseek

# 只回测Qwen
bash ~/快速重启_修复版.sh backtest-qwen

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【3. 回测+重启】⭐ 推荐（新参数立即生效）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 回测所有模型并重启所有服务
bash ~/快速重启_修复版.sh backtest-restart-all

# 回测DeepSeek并重启
bash ~/快速重启_修复版.sh backtest-restart-deepseek

# 回测Qwen并重启
bash ~/快速重启_修复版.sh backtest-restart-qwen

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【4. 查看状态】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 查看所有服务状态
supervisorctl status

# 查看DeepSeek状态
supervisorctl status deepseek

# 查看Qwen状态
supervisorctl status qwen

# 查看前端状态
supervisorctl status web

# 查看所有Python进程
ps aux | grep python | grep -v grep

# 查看端口监听
netstat -tlnp | grep -E '(80|8080)'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【5. 查看日志】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 查看DeepSeek实时日志
tail -f /root/10-23-bot/ds/logs/deepseek_trading.log

# 查看Qwen实时日志
tail -f /root/10-23-bot/ds/logs/qwen_trading.log

# 查看前端访问日志
tail -f /var/log/gunicorn/access.log

# 查看前端错误日志
tail -f /var/log/gunicorn/error.log

# 查看Supervisor日志
tail -f /var/log/supervisor/supervisord.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【6. 数据文件位置】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# DeepSeek数据目录
/root/10-23-bot/ds/trading_data/deepseek/

# Qwen数据目录
/root/10-23-bot/ds/trading_data/qwen/

# 关键文件：
  - trades_history.csv     # 历史交易记录
  - pnl_history.csv        # 盈亏历史
  - current_positions.csv  # 当前持仓
  - learning_config.json   # AI学习配置

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【7. 快速检查数据】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 查看DeepSeek最近5笔交易
tail -5 /root/10-23-bot/ds/trading_data/deepseek/trades_history.csv

# 查看Qwen最近5笔交易
tail -5 /root/10-23-bot/ds/trading_data/qwen/trades_history.csv

# 查看DeepSeek当前持仓
cat /root/10-23-bot/ds/trading_data/deepseek/current_positions.csv

# 查看Qwen当前持仓
cat /root/10-23-bot/ds/trading_data/qwen/current_positions.csv

# 统计交易数量
wc -l /root/10-23-bot/ds/trading_data/deepseek/trades_history.csv
wc -l /root/10-23-bot/ds/trading_data/qwen/trades_history.csv

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【8. 测试API接口】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 测试DeepSeek数据
curl -s 'http://localhost/trading-summary?model=deepseek' | python3 -m json.tool

# 测试Qwen数据
curl -s 'http://localhost/trading-summary?model=qwen' | python3 -m json.tool

# 测试盈亏数据
curl -s 'http://localhost/trading-pnl?model=deepseek&limit=10' | python3 -m json.tool

# 测试前端首页
curl -I http://localhost

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【9. 访问地址】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 前端界面: http://43.100.52.142 (端口80)
   - 数据已正确显示
   - 数据路径：/root/10-23-bot/ds/trading_data/

📊 Web监控: http://43.100.52.142:8080 (如果配置了)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【10. 常见问题排查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

问题1：前端无法访问
→ supervisorctl status web
→ tail -f /var/log/gunicorn/error.log
→ bash ~/快速重启_修复版.sh frontend

问题2：前端显示无数据
→ 检查数据路径是否正确（/root/10-23-bot）
→ ls -lh /root/10-23-bot/ds/trading_data/deepseek/
→ curl -s 'http://localhost/trading-summary?model=deepseek'

问题3：AI机器人不运行
→ supervisorctl status deepseek qwen
→ supervisorctl restart deepseek qwen
→ tail -f /root/10-23-bot/ds/logs/deepseek_trading.log

问题4：回测失败
→ cd /root/10-23-bot/ds
→ python3 deepseek_多币种智能版.py --backtest
→ 查看错误日志

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【11. 紧急操作】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 停止所有服务
supervisorctl stop all

# 启动所有服务
supervisorctl start all

# 重启所有服务
supervisorctl restart all

# 强制停止AI进程（慎用）
pkill -f deepseek_多币种智能版
pkill -f qwen_多币种智能版

# 强制停止前端（慎用）
pkill -f "gunicorn.*每日壁纸"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【12. 使用技巧】⭐
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  每次修改代码后：
   bash ~/快速重启_修复版.sh all

2️⃣  想看参数优化效果：
   bash ~/快速重启_修复版.sh backtest-restart-all

3️⃣  只想测试不重启：
   bash ~/快速重启_修复版.sh backtest

4️⃣  前端有问题单独重启：
   bash ~/快速重启_修复版.sh frontend

5️⃣  查看AI是否在运行：
   supervisorctl status

6️⃣  实时监控AI决策：
   tail -f /root/10-23-bot/ds/logs/deepseek_trading.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 最后更新：2025-11-12
✅ 所有命令已验证可用
🎯 数据路径已修复：/root/10-23-bot/ds/trading_data/
🚀 前端正常运行：http://43.100.52.142
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

