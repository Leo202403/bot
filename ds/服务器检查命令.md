# 服务器检查命令

## 📋 问题诊断

### 问题1：持仓时间异常
**现象**：波段0.5h < 超短线0.6h（波段反而更短）  
**诊断**：已添加DEBUG日志，下次回测会自动输出

### 问题2：共振指标为0
**现象**：邮件显示所有共振指标都是0  
**诊断**：需要检查历史数据

---

## 🔍 检查共振数据

### 命令1：运行诊断脚本

```bash
cd ~/10-23-bot/ds
python3 check_consensus_data.py
```

**该脚本会检查**：
1. ✅ 市场快照CSV中的indicator_consensus字段
2. ✅ 昨日订单CSV中的共振字段（中文+英文）
3. ✅ 显示实际数据样本
4. ✅ 统计共振值分布

**预期输出**：
```
🔍 共振数据诊断工具
======================================================================

📊 检查1：市场快照CSV文件
----------------------------------------------------------------------
✓ 找到文件: trading_data/deepseek/market_snapshots/kline_snapshots_20251118.csv
  总行数: 672
  
  关键字段检查:
    ✓ indicator_consensus      : 存在 (非零: 320/672)
    ✓ trend_15m                : 存在
    ✓ trend_1h                 : 存在
    
  前3行indicator_consensus值:
    [1] BNB      00:00      consensus=2
    [2] BTC      00:00      consensus=3
    [3] ETH      00:00      consensus=0
    
  共振值分布:
    0: 352笔 (52.4%)  ⬅️ 如果>80%说明有问题
    1: 120笔 (17.9%)
    2:  80笔 (11.9%)
    3:  60笔 ( 8.9%)

📊 检查2：昨日订单CSV文件
----------------------------------------------------------------------
✓ 找到文件: trading_data/deepseek/orders_20251118.csv
  总行数: 20
  
  关键字段检查:
    ✓ 共振指标数               : 存在 ⬅️ 关键！
    ✓ indicator_consensus      : 存在
    ✓ 信号分数                 : 存在
    
  前3笔订单数据:
    [1] 币种: BNB
        共振指标数: 3  ⬅️ 如果是0或N/A，就是问题
        indicator_consensus: 3
        信号分数: 85

🎯 诊断完成！
```

---

### 命令2：直接查看CSV文件

**查看市场快照**：
```bash
cd ~/10-23-bot/ds/trading_data/deepseek/market_snapshots
ls -lh kline_snapshots_*.csv | tail -5  # 最新5个文件
head -1 kline_snapshots_20251118.csv  # 查看字段名
head -5 kline_snapshots_20251118.csv | cut -d',' -f1,2,30-35  # 查看前5行（包含共振等字段）
```

**查看昨日订单**：
```bash
cd ~/10-23-bot/ds/trading_data/deepseek
head -1 orders_20251118.csv  # 查看字段名
cat orders_20251118.csv | grep -i "consensus\|共振"  # 查找共振相关字段
```

---

### 命令3：统计共振分布

**市场快照中的共振分布**：
```bash
cd ~/10-23-bot/ds/trading_data/deepseek/market_snapshots
python3 -c "
import pandas as pd
df = pd.read_csv('kline_snapshots_20251118.csv')
if 'indicator_consensus' in df.columns:
    print('共振值分布:')
    print(df['indicator_consensus'].value_counts().sort_index())
else:
    print('indicator_consensus字段不存在！')
"
```

**订单中的共振分布**：
```bash
cd ~/10-23-bot/ds/trading_data/deepseek
python3 -c "
import pandas as pd
df = pd.read_csv('orders_20251118.csv', encoding='utf-8-sig')
print('订单字段:', list(df.columns)[:20])
if '共振指标数' in df.columns:
    print('\n共振值分布:')
    print(df['共振指标数'].value_counts().sort_index())
elif 'indicator_consensus' in df.columns:
    print('\n共振值分布:')
    print(df['indicator_consensus'].value_counts().sort_index())
else:
    print('共振字段不存在！')
"
```

---

## 📊 下次回测会自动输出的诊断信息

### 诊断1：Phase 2权重测试
```
🔍 【DEBUG】超短线机会数据检查:
   总数: 1978
   第一个机会字段: ['coin', 'timestamp', ..., 'snapshot']
   是否有snapshot: True  ⬅️ 应该是True
   snapshot类型: <class 'dict'>

⚡ 测试超短线权重候选（共5组）...
   #1 默认 : 捕获???/1978(20-60%) ⬅️ 不再是0%
```

### 诊断2：持仓时间（每1000个机会输出一次）
```
🔍 【持仓时间诊断】第1000个机会:
   币种: BTC, 分类: 波段
   利润阈值: 10.0%
   最终利润: 12.35%
   触发bar: 15
   持仓bars: 16
   持仓小时: 4.0h  ⬅️ 观察这个值
```

### 诊断3：共振数据（邮件生成前）
```
🔍 【共振数据诊断】昨日订单字段:
   字段列表: ['币种', '方向', '开仓价格', '共振指标数', ...]
   第一笔订单:
     '共振指标数': 3  ⬅️ 如果是N/A就是问题
     'indicator_consensus': 3
     '信号分数': 85
     'signal_score': 85
```

---

## 🚀 完整回测流程

### 步骤1：运行回测
```bash
bash ~/快速重启_修复版.sh backtest-deepseek
```

### 步骤2：观察回测日志
关注以下部分：
1. Phase 2权重测试（是否有snapshot）
2. 持仓时间诊断（每1000个机会）
3. 共振数据诊断（邮件生成前）

### 步骤3：运行诊断脚本（回测完成后）
```bash
cd ~/10-23-bot/ds
python3 check_consensus_data.py
```

### 步骤4：查看回测结果
```bash
# 查看最新日志
tail -100 ~/10-23-bot/logs/deepseek_*.log

# 查看邮件内容
cat ~/10-23-bot/ds/trading_data/deepseek/last_email_content.txt
```

---

## 📝 问题判断

### 持仓时间问题

**如果诊断显示**：
```
🔍 【持仓时间诊断】第1000个机会:
   分类: 波段
   持仓小时: 0.3h  ⬅️ 波段只有0.3小时
```

**结论**：市场确实在很短时间内达到10%阈值（强趋势市场）

**解决方案**：
- 提高波段阈值（12-15%）
- 或添加时间约束

---

### 共振数据问题

**情况A：市场快照中共振值都是0**
```
共振值分布:
    0: 670笔 (99.7%)  ⬅️ 几乎全是0
    1:   2笔 ( 0.3%)
```
**结论**：indicator_consensus计算有问题  
**解决方案**：检查`recalculate_consensus_from_snapshot`函数

---

**情况B：市场快照正常，但订单中没有共振字段**
```
订单字段: ['币种', '方向', '开仓价格', ...]  ⬅️ 没有共振字段
```
**结论**：开仓时没有保存共振数据  
**解决方案**：修复开仓逻辑，确保保存indicator_consensus

---

**情况C：订单中有共振字段但值都是0**
```
[1] 币种: BNB
    共振指标数: 0  ⬅️ 都是0
    indicator_consensus: 0
```
**结论**：开仓时读取的indicator_consensus就是0  
**解决方案**：检查开仓时的数据来源

---

## 🎯 快速命令总结

```bash
# 1. 运行回测
bash ~/快速重启_修复版.sh backtest-deepseek

# 2. 运行共振诊断
cd ~/10-23-bot/ds && python3 check_consensus_data.py

# 3. 查看市场快照字段
cd ~/10-23-bot/ds/trading_data/deepseek/market_snapshots
head -1 kline_snapshots_20251118.csv

# 4. 查看订单字段
cd ~/10-23-bot/ds/trading_data/deepseek
head -1 orders_20251118.csv

# 5. 统计共振分布
python3 -c "import pandas as pd; df=pd.read_csv('orders_20251118.csv', encoding='utf-8-sig'); print(df['共振指标数'].value_counts() if '共振指标数' in df.columns else '字段不存在')"
```

---

**✅ 诊断工具已就绪！** 🔍

