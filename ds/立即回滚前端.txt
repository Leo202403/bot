━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
V8.3.21 前端紧急回滚操作
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚨 问题现象：
   前端综合页面显示 "Not Found"，DeepSeek 可以看到 AI 决策，但 Qwen 不能

🎯 解决方案：
   回滚到之前可以正常工作的备份版本

📋 执行步骤：

1️⃣  在服务器上执行以下一键命令：

cd /root/10-23-bot && git pull origin main && bash ds/服务器前端回滚.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 预期结果：
   - 找到最近的备份文件（如 每日壁纸更换.py.backup.20251111_xxx）
   - 恢复备份文件
   - 重启 web 服务
   - 端口 80 正常监听
   - 刷新浏览器，页面恢复正常

⚠️  如果提示"未找到备份文件"：
   说明之前的 更新前端修复.sh 脚本没有执行过，或者备份文件被删除了
   
   这种情况下，请直接执行：
   
   cd /root/pythonc程序/my_project
   git log -1 每日壁纸更换.py  # 查看最近一次修改
   git checkout HEAD~1 每日壁纸更换.py  # 回退到上一个版本
   supervisorctl restart web

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 验证修复：
   1. 访问前端综合页面
   2. 检查是否还显示 "Not Found"
   3. 查看 AI 决策栏是否正常显示
   4. 切换到 Qwen 和 DeepSeek 单独页面测试

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

